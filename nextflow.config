
process {
  withName:run_bridges_pipeline {
    container = 'r-env'  // or conda = 'bridges-env'
  }
}

cleanup {
    enabled = true
    onSuccess = true    // Clean up on successful completion
    onFailure = false   // Keep work files if pipeline fails (for debugging)
}